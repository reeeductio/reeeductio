1. ~~Add local cache / storage for messagess~~
2. Admin CLI for configuring OPAQUE
3. CLI support for auth with OPAQUE
4. Add user symmetric key
5. Make symmetric keys optional, to support admin tools that only modify (plaintext) state
   - User symmetric key
   - Symmetric root
